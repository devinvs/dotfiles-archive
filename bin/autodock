NUM_MONS=$(xrandr --listmonitors | head -n 1 | cut -d' ' -f2)

if [ $NUM_MONS -gt 1 ]
then
  echo Docking...
  dock
else
  echo Undocking...
  xrandr --output DP-0.1 --off --output DP-0.2 --off --output DP-2 --auto
fi
